Local model weights should be placed here.

- Put large model files (e.g., `.pt`, `.onnx`) in this folder.
- Do NOT commit model weights to git; store them separately and add download instructions here.

Example:

- `models/yolov5_chess.pt` — YOLOv5 weights for piece detection
